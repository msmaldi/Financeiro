@model IEnumerable<Msmaldi.Financeiro.Website.BusinessLogic.SwingTrade.PosicaoConsolidadaSwingTrade<SwingTrade>>
@{ ViewData["Title"] = "Swing Trade"; }
<h2>@ViewData["Title"]</h2>
<p><a asp-action="Create">Incluir novo</a></p>
<hr />
<div class="row">
@foreach (var item in Model)
{
    <div class="col-lg-4 col-md-6 col-sm-6 col-xs-12">
        <div class="panel panel-default">
            <div class="panel-heading">
                Swing Trade
            </div>
            <div class="panel-body">
                <dl class="dl-horizontal">
                    <dt>Ativo:</dt>
                    <dd>@item.SwingTrade.Symbol</dd>
                    <dt>Pre&ccedil;o Unit&aacute;rio:</dt>
                    <dd>@item.SwingTrade.ValorDeAquisicao.ToString("N2")</dd>
                    <dt>Quantidade:</dt>
                    <dd>@item.SwingTrade.Quantidade</dd>
                    <dt>Valor Bruto:</dt>
                    <dd>@item.ValorBruto.ToString("N2")</dd>
                    <dt>Rendimento Bruto:</dt>
                    <dd>@item.RendimentoBruto.ToString("N2")</dd>
                </dl>
                <div class="text-center">
                    <a class="btn btn-default" asp-action="Delete" asp-route-id="@item.SwingTrade.Id">Excluir</a>
                </div>
            </div>
        </div>
    </div>
}
</div>